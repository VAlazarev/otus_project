
Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Если  Состояние=Перечисления.СостоянияЗаказов.Выполнен Тогда 
		МенеджерЗаписи= РегистрыСведений.ОчередьСообщенийКОтправкеWMS.СоздатьМенеджерЗаписи(); 
		Json=РаботаСОчередями.СериализоватьВJson(Ссылка);
		МенеджерЗаписи.Сообщение= Json; 
		МенеджерЗаписи.ID= Новый УникальныйИдентификатор; 
		МенеджерЗаписи.Записать();
		
	КонецЕсли;

КонецПроцедуры
